1. Selecting more than 1 tile (if single select is used) should work only when you're pressing CRTL, not just normally. Change it .
2. Make block selecting.
3. Auto-selector.
4. Context menu options.

    Środek zaznaczenia zmienia swoje miejsce przy zmienach połówek osi,
    jak zeskrolujesz strone to nie działa zaznaczenie tak jak ostatnio (naprawiłeś to wcześniej).